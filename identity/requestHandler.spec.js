describe('request handler takes requests and writes request accepted events', ()=>{
    it('will write request Accepted into request stream', ()=>{

       // requestHandler(store, {data:{name:'dave'}})
       expect(1).toBe(1)
    })
})

// describe('requestToInformationHandler takes accepted requests, gets the info and writes a request completed event', ()=>{
//     it('will read a requestAcceped event', ()=>{

//     })
// })